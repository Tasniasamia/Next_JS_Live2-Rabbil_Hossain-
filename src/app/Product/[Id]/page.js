import React from 'react';

const page = ({searchParams}) => {
    console.log(searchParams.Id);
    
    return (
        <div>
            Product details
        </div>
    );
};

export default page;
